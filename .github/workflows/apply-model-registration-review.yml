name: Apply reviewed model registration workflow

on:
  push:
    branches:
      - agent/p0-runtime-integrity

permissions:
  contents: write

concurrency:
  group: reviewed-model-registration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out runtime-integrity branch
        uses: actions/checkout@v4
        with:
          ref: agent/p0-runtime-integrity
          fetch-depth: 0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.4

      - name: Apply reviewed model registration integration
        run: |
          node scripts/apply-model-registration-review.mjs
          node scripts/fix-model-registration-review-integration.mjs

      - name: Remove one-time migration tooling
        run: |
          git rm -f \
            .github/workflows/apply-model-registration-review.yml \
            scripts/apply-model-registration-review.mjs \
            scripts/fix-model-registration-review-integration.mjs

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun run test

      - name: Lint
        run: bun run lint

      - name: Build standalone app
        run: bun run build:app

      - name: Commit reviewed registration workflow
        run: |
          if git diff --quiet; then
            echo "No reviewed registration changes to commit"
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "add reviewed custom model registration workflow"
          git push origin HEAD:agent/p0-runtime-integrity
